```
int maxVal;  // 全局变量，记录最大路径和

int dfs(struct TreeNode* root) {
    if (!root) return 0;

    // 递归拿到左右子树的最大贡献（负数就不要，取 0）
    int left  = dfs(root->left);
    int right = dfs(root->right);
    if (left < 0)  left = 0;
    if (right < 0) right = 0;

    // 第一件事：尝试在当前节点"拐弯"，更新全局最大值
    int throughMe = left + right + root->val;
    if (throughMe > maxVal) maxVal = throughMe;

    // 第二件事：返回给父节点（只能选一边）
    return (left > right ? left : right) + root->val;
}

int maxPathSum(struct TreeNode* root) {
    maxVal = -1001;  // 题目说节点值 >= -1000，所以初始化比这更小
    dfs(root);
    return maxVal;
}
```

