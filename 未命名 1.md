这是我们计算机体系结构课程的小组作业，我们小组的研究主题是PIM，main里面是我们小组汇报的最终版本的PPT，我负责的部分：Why Attention is Memory-Bound，Traditional Execution vs. AttAcc，Mapping Attention to PIM Hardware，Attention Dataflow and Partitioning in PIM和Why Attention Benefits from PIM。明天我们要进行汇报的排练，我们小组的汇报总时长为25分钟，我负责的部分需要有2.5分钟，并且需要全英文汇报。请仔细思考。



-  In the following part, I will focus on attention, which is actually the main source of this bottleneck.

- 1First, let’s understand the problem.  
	In autoregressive decoding, attention is mainly dominated by **GEMV operations**, which have very low arithmetic intensity. At the same time, the model needs to repeatedly access large **key and value matrices**.  
	As a result, attention is not limited by computation, but by **memory bandwidth**

- 2Now, based on this observation, let’s look at how attention is executed traditionally. 
	On GPUs, KV data must be moved back and forth between DRAM and the compute units. This creates a long data path and makes off-chip bandwidth the main bottleneck.
	- To address this issue, AttAcc changes the execution paradigm.  
		Instead of moving data to the compute units, it executes attention **directly inside memory**. This keeps KV matrices close to where the computation happens and avoids unnecessary data movement.

- 3Next, let’s see how attention is mapped onto PIM hardware.  
	Attention can be decomposed into three steps:  
	first, computing scores with Q times K transpose;  
	then applying Softmax;  
	and finally generating the context by multiplying with V.  
	In PIM, the two GEMV steps are executed inside DRAM banks, while Softmax is handled in the buffer layer.

- 4This leads to a very efficient dataflow.  
	The query enters memory, while K and V remain in DRAM throughout the execution. Only the final result is returned to the processor.  
	So most of the KV data never leaves memory.

- 5 Finally, this is why attention benefits greatly from PIM.  
	Its computation pattern matches well with DRAM-level parallelism, and more importantly, PIM minimizes data movement, which is the real bottleneck of attention.

- So in summary, attention is not a compute problem, but a data movement problem, and PIM directly addresses this challenge.


Good afternoon everyone.  
- In the following part, I will focus on attention, which is actually the main source of this bottleneck.

- 1 First, let’s understand the problem.  
	In autoregressive decoding, attention is mainly dominated by **GEMV operations**, which have very low arithmetic intensity. At the same time, the model needs to repeatedly access large **key and value matrices**.  
	As a result, attention is not limited by computation, but by **memory bandwidth**.

- 2 Now, based on this observation, let’s look at how attention is executed traditionally.  
	On GPUs, KV data must be moved back and forth between DRAM and the compute units. This creates a long data path and makes off-chip bandwidth the main bottleneck.
	To address this issue, AttAcc changes the execution paradigm.  
	Instead of moving data to the compute units, it executes attention **directly inside memory**. This keeps KV matrices close to where the computation happens and avoids unnecessary data movement.

- 3 Next, let’s see how attention is mapped onto PIM hardware.  
	Attention can be decomposed into three steps:  
	first, computing scores with Q times K transpose;  
	then applying Softmax;  
	and finally generating the context by multiplying with V.  
	In PIM, the two GEMV steps are executed inside DRAM banks, while Softmax is handled in the buffer layer.

- 4 This leads to a very efficient dataflow.  
	The query enters memory, while K and V remain in DRAM throughout the execution, and only the final result is returned to the processor.  
	So most of the KV data never leaves memory.

- 5 Finally, this is why attention benefits greatly from PIM.  
	Its computation pattern matches well with DRAM-level parallelism, and more importantly, PIM minimizes data movement, which is the real bottleneck of attention.

- So in summary, attention is not a compute problem, but a data movement problem, and PIM directly addresses this challenge.

Thank you.



首先，异常和中断是什么？
异常是在 处理器 顺序执行 程序指令流 的过程中 突然遇到了异常的事情 而中止执行当前的程序，转而去处理异常。
中断是在 处理器 顺序执行 程序指令流 的过程中 突然被别的请求打断 而中止执行当前的程序，去处理别的事情。
中断与异常的区别在于来源：中断往往是外因引起的，比如键盘，时钟，外设造成的中断，异常是内因引起的，即正在执行的程序发生了错误和异常，比如page fault, 非法指令，地址越界等。
广义的异常包括中断，后面可能会用异常指代中断和异常

而因为中断和异常导致的控制流切换我们可称之为trap
当在程序的运行过程中，遇到了中断或者异常，就会发生trap，此时会强制跳转到一个专门处理相应问题的程序片段，我们可以称之为trap handler，等到处理完成后再跳转回原来的程序继续执行。对于一个支持多种特权模式的系统，我们平日里使用的应用程序和trap handler通常是处于不同的特权等级的，因此在这个过程中会发生特权等级的切换。

那么什么是特权等级呢？

==end==

【造成特权模式切换的一个重要原因是发生Trap。比如，在用户模式下运行应用程序时，如果发生某些trap（例如一个管理员调用或者一个定时器中断），便会强制跳转到一个专门处理相应问题的程序片段，我们可以称之为trap handler，它通常运行在更高的特权模式下。然后等到它执行完后再切换回原来的特权模式并跳回原来的指令处继续应用程序的执行。当然，trap也不一定会发生特权模式切换，提升特权级别的trap称为垂直自陷（vertical trap），而保持在同样特权级别的trap称为水平自陷（horizontal trap）。】


hart 硬件线程


因为程序运行的安全性不可被充分信任，为了确保系统的安全性，需要定义一些特权等级，每个特权模式对资源有不同的操作权限。
RISC-V 架构中一共定义了 4 种特权模式，U，S，H，M
一般而言，这四个模式对应的权限是从上到下依次提升的，用户的应用程序一般运行在U模式，而M是最高的特权模式, 它拥有对机器底层的一切访问权限
（点）不同复杂程度的系统支持的特权等级组合会存在不同，一般存在下表这三种组合模式，其中m模式是必须的
接下来我们来具体看一下中断异常的处理流程


中断异常的处理流程总体上可以概括为三步：
在这里我们假设instruction2这里发生异常
（点）当我们沿着程序运行到异常时，
（点）第一步，我们保存中断异常信息，然后跳转到对应的异常处理程序
（点）第二步，执行这个处理程序
（点）结束之后，最后一步跳转回原程序继续执行

而中断和异常的处理过程中有一个区别，
对于异常，跳回原程序会重新执行发生异常的指令，
但是对于中断，是回到中断发生时那条指令的下一条指令
这一点也很好理解，异常是内因引起的，也就是说指令本身的执行出了问题，比如一条指令执行时遇到了page fault, 这条指令是不会完成的，等到处理完了page fault异常之后，需要再次执行一下出现问题的指令，因此是回到触发异常的那条指令
而中断是外因引起的，在进入中断处理程序时，我们会保证当前指令执行完成，因此中断处理完了之后，恢复下一条指令的执行就可以了



刚刚我们提到处理异常和中断的过程中第一个步骤就是保存中断异常的信息，而保存这个信息的地方就是控制状态寄存器CSR
CSR是支撑 RISC-V 特权架构的一个重要概念。
它是 CPU 中的一系列特殊的专用寄存器，这些寄存器能够反映和控制 CPU 当前的状态和执行机制。
我们实验中处理中断异常时就需要读写相应的CSR寄存器

在之前的实验中我们实现的通用寄存器是5位地址空间，一共是32个寄存器
而CSR 的地址空间有 12 位，因此理论上能够支持最多 4,096 个 CSR。但实际上，这个地址空间大部分是空的，访问不存在的 CSR 将触发无效指令异常。
CSR地址格式如左图所示，其中在这12位CSR地址中，最高两位表明了读写权限，接下来两位表明哪个特权模式可以使用该CSR

右图是CSR和对应的地址范围，供大家查看参考

（因为我们之前只用到了通用寄存器GPR, 现在对比来看CSR和GPR ，CSR和GPR就是不同用途的寄存器，如果我们还要支持浮点运算的话，还会用到FPR）

这里给出了机器模式下的csr的地址，名称和一些简要描述，我们实验中不会全部用到，大家可以看看了解一下
（可以去读riscv privilege architecture）

然后，这里展示的就是比较重要的几个CSR，在我们这个实验里面只有机器模式，所以关心的都是机器模式下的csr, 我们也可以看到这些都是 m 开头的，意味着machine
接下来我们会依次介绍本此实验会涉及到的几个CSR

（如果是关心supervisor模式下的csr的话，还有sstatus, sie等寄存器）

首先是mstatus寄存器，它记录了当前处理器的状态。可以看到这个寄存器有很多字段（field），我们只关心其中几个字段,
首先是后缀为IE的字段，像这里框出来的MIE、SIE、UIE，它们表示某个特权模式下的中断使能，其中第一个字母代表着这具体的哪个模式下的中断使能
比如，MIE是机器模式中断使能，MIE为1时机器模式中断打开，为0时中断关闭



后缀为PIE的字段保存了trap发生之前的中断使能位
比如MPIE，就是trap发生前机器模式的中断使能
后缀为PP的字段保存了trap发生之前的特权等级
我们的实验中只有机器模式，因此只涉及到M开头的几个字段，
具体而言，在进入异常和退出异常的时候，mstatus寄存器会做这些操作
进入的时候，MPP会存储异常发生前的特权模式，不过我们在实验中可以忽略这一步，因为我们的实验全程运行在机器模式下，不会发生特权模式切换；
然后是黄色的字体，表示MPIE会存储异常发生前的机器模式中断使能；最后是绿色的字体，指将MIE置0，也就是说全局中断被关闭，此时再发生的中断异常不会被响应
退出中断异常的时候，MIE会恢复到MPIE的值，MPIE会置为1，特权模式恢复到MPP保存的值
第二个介绍的是mcause寄存器
Mcause寄存器存储了异常发生的原因。在当 trap 进入机器模式后，会将中断异常产生的起因写入到该寄存器中
Mcause最高位表明这是否为一个中断，值为1表示为中断，为0指发生了异常
我们实验只关注标识出来的这几个
首先是一个中断
然后是异常中的
非法指令
Load/Store 异常
以及 ECALL

第三个介绍的是，mtvec寄存器
RISCV处理器进入中断异常后，要跳入中断异常处理程序，而跳入的目标地址会被提前存储到mtvec这个寄存器中
目前 RISC-V 支持两种类型的地址表示方式，分别是直接模式和向量化模式，这是根据最后两位mode位来决定的：
直接模式（direct），所有类型的中断异常均会直接跳转到存储的这个基地址。即直接将PC赋值为base, 
在向量化模式下，所有的 异常 仍然会直接跳转到存储的这个基地址，但 中断 将根据 Cause（也就是中断产生的原因）跳转到不同的地址
为了简化实现，我们的实验中采用直接模式

第四个是mepc寄存器，它存储了处理完中断异常后的返回地址。
在异常发生时，硬件会自动更新mepc的值为该异常指令的地址
中断发生时mepc被更新为遇到中断的指令的下一条指令的地址
这里赋值不同的原因我们也在之前解释过了
而当中断异常处理完return时，mepc的值会被赋给PC

（点）其中还有一个问题是ecall这类指令，在刚刚介绍mcause表中我们会发现ecall指令触发的是异常，因此当遇到ecall指令时，mepc会被设置为当前指令的地址，也就是这个ecall指令的pc, 但如果异常处理完成之后再回到mepc中存储的地址, 也就是ecall指令的话，便会导致无限循环
（点）针对这个问题，解决方法其实是当ecall发生时，会通过软件将mepc设置为ecall的下一条指令的地址
刚刚介绍的这些CSR寄存器都需要特定的CSR指令来操作，
而在一段异常处理程序中，不可避免的会有读写mstatus, mepc等CSR寄存器的操作，此时我们就需要使用CSR指令
这里是csr指令的编码格式

这里介绍了我们需要使用的几条CSR指令，
从上到下分别是对指定的csr进行写操作，将指定的CSR中的特定位置为1，将指定的CSR中的特定位置为0
下面的三条功能也是相同的，不过是使用了立即数
之后大家可以看看这一页仔细理解一下每一条指令的具体细节


除此之外还有一些特权指令
ECALL指令用于向执行环境发出请求，在不同的特权等级中执行ECALL指令有不同的效果：在User Mode中会引发environment-call-from-U-mode异常，在Supervisor Mode中会引发environment-call-from-S-mode异常, 而在Machine Mode中会引发environment-call-from-M-mode异常。

当异常处理程序完成异常处理之后，需要返回原先的中断点继续执行原程序。RISC-V架构针对三种特权模式分别定义了专门的退出指令(Trap-Return Instructions)，包括MRET、SRET和URET。其中MRET指令是机器模式下的退出异常指令，也是必备的指令。在我们的实验中，异常处理程序用 mret 指令（M 模式特有的指令）返回, mret 指令会将 PC 设置为 mepc，通过将 mstatus 的 MPIE 字段复制到MIE 来恢复之前的中断使能设置，并将权限模式设置为 mstatus 的 MPP 字段中的值。
接下来我们总结一下trap的处理流程，也就是最开始我们说的三步
首先进入trap
停止当前程序的执行流，
1.将pc设置为mtvec指向的地址
2.更新csr寄存器，mstatus, mepc, mcause
3.保存中断异常的相关信息
第二部执行中断异常处理程序，这里展示的是我们本实验仿真使用的一段代码
第三步，处理完了之后
将pc设置为mepc指向的地址，恢复csr寄存器mstatus
在了解了前面的知识后，在单周期cpu中实现中断和异常没有太大问题
但是我们现在实现的是流水线CPU，在流水线cpu中支持中断异常显然会遇见新的问题，比如我们应该在哪个流水级去处理中断异常呢
在此之前，需要先提出一个精确异常的概念，这个概念是说faulting instruction之前的指令都需要完成，而之后的指令不能改变机器的状态

（点）为了实现精确异常，首先要考虑的是在什么时候去处理中断异常
不同的流水级可能发生不同的异常，比如IF阶段可能发生取址异常，ID阶段decode发现是非法指令，执行阶段计算异常，内存阶段load store发生memory异常
现在的问题是，这些异常一旦出现，我们是否需要马上处理呢？

（点）我们这里来看一个例子，指令1和指令2按顺序执行，指令1执行到memory阶段出现了异常，而指令2执行到解码阶段就发生了异常。按照我们观察到异常的时间顺序来说，我们会先观测到指令2发生了异常，再观测到指令1的异常。
这种情况下，一出现异常就立即处理肯定是不合适的，因为在指令1还没有正确执行的情况下，先处理指令2的异常是没有意义的，还可能会影响程序执行的正确性

结合我们上面说的情况，最终我们选择发生异常后将异常信息携带在指令上沿着流水线一路传递下去，直到write back阶段才真正去处理异常，因为write back阶段以后至少不会产生新的异常，刚刚这个例子所面临的问题也迎刃而解

但其实WB阶段处理异常还是有一点小问题的，比如RAM的写操作是在MEM阶段就已经完成的，但我们实验提供的代码中没有考虑这个问题，大家实验时可以忽略掉
如果要解决这个问题，可选的方案是在memory阶段检查一下有没有狭义异常产生

==end==

* 写回指令报出异常的同时，清空所有流水级的缓存，并将PC置为异常入口地址

这是实验2的架构图，我们这次实验的重点在于上方这个Exception Unit
这个模块主要有两个作用
发生中断异常时，对应指令运行到WB阶段会将相关的信息传递给这个模块，由这个模块来控制流水线进入中断异常的处理程序，完成后再控制跳出
第二个是用于处理csr指令，这个模块在csr指令执行的内存存取阶段用来控制相应的csr寄存器

从图中我们也可以看到，这里面中断异常会在wb阶段开始处理，即指向exception unit模块底部的那些箭头，红线随着流水线一路执行，直到WB阶段才将信息传递给Exception Unit
但是一些csr指令，mret指令会在memory阶段开始处理，对应的输入为指向模块右侧的那些箭头
然后我们来看一下具体的代码
Exception Unit这个模块的主要任务
首先是进入异常时，它要完成mstatus等寄存器的读写
然后是异常服务程序中，负责执行csr指令，读写相应的csr寄存器
最后是退出异常时，完成mstatus等寄存器的恢复

在给出的代码中可以看到CSR寄存器是包含在这个Exception Unit中的，也就是右下角那块代码

然后具体看一下这个模块的输入输出，
（点）这一部分是csr读写所需的输入输出，
（点）这一部分是中断异常的相关信息，
这是mret指令，
（点）而这两个epc是用来在中断异常发生的时候设置mepc的，
然后是输出，
（点）这是将pc重定向到mtvec或者是mepc里保存的地址的,用来控制进入和退出trap的 
（点）然后这些是用来flush流水线寄存器的，具体的使用方式和实验1是相似的，最后一个是用来控制flush WB阶段写寄存器的，在异常出现的时我们需要flush掉这个寄存器



然后这是顶层模块里面给的exception unit的参数，这一次代码中是都写好了的，大家对照着看就行

第一部分是用来执行csr指令的，第二部分是用来处理中断异常的
通过变量名的后缀我们可以看到csr和mret部分是在memory阶段做的，它们的后缀是MEM，代表它们是memory阶段的信号
中断异常的处理是在Write back阶段做的，后缀是WB
接下来我们来看一下exception unit模块的具体实现方式
首先来看一下已经给出的CSR寄存器的输入输出，可以看到是只有一个读端口和一个写端口的
但我们进入trap其实是要读写多个csr寄存器的，比如mepc, mstatus, mcause这些
这意味着我们不能在一个周期内进行多次的读写，只能分周期来做

分周期后我们设计这样一个有限状态机图
这个状态机有3个状态，STATE_IDLE, STATE_MEPC, STATE_MCAUSE
接下来我们来看一下这个状态机的状态是怎么转移的

首先看一下如何进入中断异常，这个过程中，状态机需要走一遍1, 2, 3这条路线
第一步是STATE_IDLE到STATE_MEPC, 这一步过程中会写mstatus寄存器，flush掉流水线寄存器, 记录epc和cause，如果是异常不是中断的话，取消write back阶段写寄存器的行为
第二步从STATE_MEPC到STATE_MCAUSE, 这一步会将第一步记录的epc写到mepc中，读mtvec寄存器，并且重定向pc，下一个周期pc就会从mtvec开始执行
第三步从STATE_MCAUSE到STATE_IDLE, 这一步会将cause写入mcause寄存器

而退出异常时，状态机走的是第4步
恢复mstatus寄存器，读mepc寄存器，重定向pc, flush掉一些pipeline register

对于中断异常处理程序中操作csr寄存器的相关指令则走第5步

剩下的情况下，状态机会走第6步，此时不进行任何操作
这里先使用hdlbits上的一个例子来给大家展示状态机的实现

这份代码实现的状态机如右下角所示，有两个状态A, B, 除了clock reset外还有一个输入, 和一个输出out

在代码里面，首先定义了两个状态A和B, 然后使用了两个寄存器，state存储当前状态，next_state存储下一周期应该转移到的状态

接下来是三块逻辑
第一个always引导的是state transition block，它决定下一状态是什么,
第二个always引导的是state flip-flops，它来改变现在的状态, 
最后是输出
状态机写法有很多，不局限于这里展示的这一种，这里仅作为参考


（有一段式两段式三段式，这是看状态机中always block的数量
这个应该是属于两段式写法，大家也可以去看看其他写法）
接下来回到我们本次实验，来Exception Unit的状态机实现
首先这里是一个状态定义，有三个状态IDLE, MEPC, MCAUSE, 
然后是两个Reg变量，分别来存储现在的状态和下一周期应该转移到的状态
（点）然后这是一个State flip-flops，用来转换现在的状态
后面的代码是一个state transition logic, 用来决定下一状态是什么，它描述了我们刚刚讲的六种不同操作，我们也是在这里给csr寄存器模块的输入信号赋值，以此来读写csr寄存器
exception unit最后还有一些输出逻辑，包括pc重定向，流水线寄存器的flush
然后这是这次实验仿真使用的代码
因为中断是外因引起的，我们的测试代码只能模拟异常，所以中断的测试需要同学们自己操作板上对应的switch来制造中断
注意一下第12条指令这里，程序运行到这里才设置了mtvec的值，大家测试中断的时候要在程序运行完这条指令之后再测试，不然会出错
最后实验验收的时候，我们还是会检查仿真波形图和上版结果，然后这里提示一下大家测试中断的时候是操作switch 12，然后Code2Inst.v这个文件中缺少CSR指令，大家可以补充一下，补充方式和实验一是一样的
接下来我们来看一下工程文件
