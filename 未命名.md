```
`timescale 1ps/1ps

module HazardDetectionUnit(
    input clk,
    input Branch_ID, rs1use_ID, rs2use_ID, cmp_res_ID, JALR_ID, JAL_ID,
    input[1:0] hazard_optype_ID,
    input[4:0] rd_EXE, rd_MEM, rs1_ID, rs2_ID, rs2_EXE,
    input[31:0] inst_EXE,                              // EXE阶段指令，用于判断是否为Load
    output PC_EN_IF, reg_FD_EN, reg_FD_stall, reg_FD_flush,
        reg_DE_EN, reg_DE_flush, reg_EM_EN, reg_EM_flush, reg_MW_EN,
    output forward_ctrl_ls,
    output[1:0] forward_ctrl_A, forward_ctrl_B
);

    // Load-Use冒险检测
    // 判断EXE阶段指令是否为Load指令（opcode = 7'b0000011）
    wire is_load_EXE = (inst_EXE[6:0] == 7'b0000011) & (rd_EXE != 0);
    
    // Load-Use冒险：EXE阶段是Load，且ID阶段指令需要读取该Load的目的寄存器
    wire load_use_A = is_load_EXE & (rd_EXE == rs1_ID) & rs1use_ID;
    wire load_use_B = is_load_EXE & (rd_EXE == rs2_ID) & rs2use_ID;
    wire load_use_hazard = load_use_A | load_use_B;
    
    // 分支决策逻辑（与RV32core的jump_condition保持同步）
    // 无条件跳转(JAL/JALR)总是taken
    // 条件分支(B_valid)需要cmp_res_ID=1才taken
    wire B_valid = ~JALR_ID & ~JAL_ID & Branch_ID; // 条件分支
    wire branch_taken = (B_valid & cmp_res_ID) | JAL_ID | JALR_ID;
    
    // RAW冒险检测
    wire raw_A_EXE = (rd_EXE == rs1_ID) & rs1use_ID & (rd_EXE != 0);
    wire raw_B_EXE = (rd_EXE == rs2_ID) & rs2use_ID & (rd_EXE != 0);
    wire raw_A_MEM = (rd_MEM == rs1_ID) & rs1use_ID & (rd_MEM != 0);
    wire raw_B_MEM = (rd_MEM == rs2_ID) & rs2use_ID & (rd_MEM != 0);
    
    // Store转发检测
     wire store_forward = (rd_MEM == rs2_EXE) && (rd_MEM != 0);
    
    // 转发控制信号
    // forward_ctrl_A: 00=来自寄存器, 01=来自WB, 10=来自EXE, 11=来自MEM
    // 转发优先级: MEM > EXE > 寄存器(默认00)
    assign forward_ctrl_A = ({2{raw_A_MEM}} & 2'b11) |
                            ({2{raw_A_EXE & ~raw_A_MEM & ~load_use_A}} & 2'b10);

    // forward_ctrl_B: 00=来自寄存器[默认], 10=来自EXE, 11=来自MEM
    assign forward_ctrl_B = ({2{raw_B_MEM}} & 2'b11) |
                            ({2{raw_B_EXE & ~raw_B_MEM & ~load_use_B}} & 2'b10);

    // Store数据转发
    assign forward_ctrl_ls = store_forward;
    
    // Load-Use冒险导致stall（需要stall一个周期）
    wire need_stall = load_use_hazard;
    
    // 控制信号生成
    // Stall情况：出现Load-Use冒险
    assign PC_EN_IF = ~need_stall;           // PC使能，stall时不更新
    assign reg_FD_EN = ~need_stall;          // IF/ID寄存器使能，stall时不更新
    assign reg_FD_stall = need_stall;        // stall信号，表示输入保持不变
    
    // 分支冒险处理：当分支被taken时，清空IF/ID和ID/EX中的错误指令
    // JAL/JALR无条件跳转或条件分支taken时都需要flush
    assign reg_FD_flush = branch_taken;      // 分支跳转时flush IF/ID寄存器
    
    // ID/EX和EX/MEM阶段也需要flush以清空流水线
    assign reg_DE_EN = 1;                    // ID/EX寄存器总是使能
    assign reg_DE_flush = branch_taken;      // 分支跳转时flush ID/EX
    
    assign reg_EM_EN = 1;                    // EX/MEM寄存器总是使能
    assign reg_EM_flush = branch_taken;      // 分支跳转时flush EX/MEM
    
    assign reg_MW_EN = 1;                    // MEM/WB寄存器总是使能

endmodule

```


https://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@github.com/Solitudkoi/obsidian_notes.git





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

Solitudkoi