**Least Recently Used (LRU)** cache scheme is to remove the least recently used frame (the one hasn't been used for the longest amount of time) when the cache is full and a new page is referenced which is not there in cache.

LRU-K is a variant of the LRU algorithm, where K represents the number of recent uses. LRU can be considered as LRU-1. Unlike LRU, the LRU-K requires the maintenance of two different queues (for historical access and cache). The data in the historical access queue is not moved to the cache queue until the data is hit K times.

For example, assuming that the length of both queues is 5, and the memory is initialized to be empty. LRU-2 is used to process the data sequence in order: 9，5，6，7，8，3，8，9，5，9，8，3，4，7，5，6. The changes of the historical access queue and the cache queue are shown in the following table:


最近最少使用（LRU）缓存方案是指，当缓存已满且引用了一个不在缓存中的新页面时，移除最近最少使用的帧（即最长时间未被使用的帧）。
LRU - K是LRU算法的一种变体，其中K表示最近使用的次数。LRU可以被视为LRU - 1。与LRU不同，LRU - K需要维护两个不同的队列（用于历史访问和缓存）。历史访问队列中的数据在被命中K次之前，不会移动到缓存队列中。
例如，假设两个队列的长度均为5，并且内存初始化为空。使用LRU - 2按顺序处理数据序列：9，5，6，7，8，3，8，9，5，9，8，3，4，7，5，6。历史访问队列和缓存队列的变化如下表所示。 

每个输入文件包含一个测试用例。对于每个用例，第一行给出3个正整数：（）、（）和（），分别是缓存命中次数、队列大小（假设两个队列大小相同）以及引用的页面ID数量。然后在下一行给出  个引用的页面ID。页面ID是一个在  范围内的数字。一行中的所有数字均以空格分隔。

对于每个测试用例，在第一行输出历史访问队列中的页面ID，在第二行输出缓存队列中的页面ID。ID按照每个队列从队首到队尾的顺序排列。一行中的所有数字必须用一个空格分隔，且行首和行尾不能有多余的空格。如果某个队列为空，则在一行中输出 - 。





When the input is much too large to fit into memory, we have to do **external sorting** instead of internal sorting. One of the key steps in external sorting is to generate sets of sorted records (also called **runs**) with limited internal memory. The simplest method is to read as many records as possible into the memory, and sort them internally, then write the resulting run back to some tape. The size of each run is the same as the capacity of the internal memory.

**Replacement Selection** sorting algorithm was described in 1965 by Donald Knuth. Notice that as soon as the first record is written to an output tape, the memory it used becomes available for another record. Assume that we are sorting in ascending order, if the next record is not smaller than the record we have just output, then it can be included in the run.

For example, suppose that we have a set of input { 81, 94, 11, 96, 12, 99, 35 }, and our memory can sort 3 records only. By the simplest method we will obtain three runs: { 11, 81, 94 }, { 12, 96, 99 } and { 35 }. According to the replacement selection algorithm, we would read and sort the first 3 records { 81, 94, 11 } and output 11 as the smallest one. Then one space is available so 96 is read in and will join the first run since it is larger than 11. Now we have { 81, 94, 96 }. After 81 is out, 12 comes in but it must belong to the next run since it is smaller than 81. Hence we have { 94, 96, 12 } where 12 will stay since it belongs to the next run. When 94 is out and 99 is in, since 99 is larger than 94, it must belong to the **first run**. Eventually we will obtain two runs: the first one contains { 11, 81, 94, 96, 99 } and the second one contains { 12, 35 }.

Your job is to implement this replacement selection algorithm.

### Input Specification:

Each input file contains several test cases. The first line gives two positive integers N (≤105) and M (<N/2), which are the total number of records to be sorted, and the capacity of the internal memory. Then N numbers are given in the next line, all in the range of **int**. All the numbers in a line are separated by a space.

### Output Specification:

For each test case, print in each line a run (in ascending order) generated by the replacement selection algorithm. All the numbers in a line must be separated by exactly 1 space, and there must be no extra space at the beginning or the end of the line.

### Sample Input:

```in
13 3
81 94 11 96 12 99 17 35 28 58 41 75 15
```

### Sample Output:

```out
11 81 94 96 99
12 17 28 35 41 58 75
15
```

当输入数据太大以至于无法全部放入内存时，我们必须使用外部排序而非内部排序。外部排序的关键步骤之一是利用有限的内部内存生成已排序记录集（也称为顺串）。最简单的方法是将尽可能多的记录读入内存，在内部进行排序，然后将生成的顺串写回某个磁带。每个顺串的大小与内部内存的容量相同。
替换选择排序算法由唐纳德·克努特（Donald Knuth）于1965年提出。请注意，一旦第一条记录被写入输出磁带，其所占用的内存就可用于存储另一条记录。假设我们按升序排序，如果下一条记录不小于我们刚刚输出的记录，那么它就可以包含在当前顺串中。
例如，假设我们有一组输入数据{81, 94, 11, 96, 12, 99, 35}，而我们的内存只能对3条记录进行排序。通过最简单的方法，我们将得到三个顺串：{11, 81, 94}、{12, 96, 99}和{35}。根据替换选择算法，我们将读取并对前3条记录{81, 94, 11}进行排序，并输出最小的11。然后有一个空间可用，因此读取96，由于96大于11，它将加入第一个顺串。现在我们有{81, 94, 96}。81输出后，12读入，但由于12小于81，它必须属于下一个顺串。因此我们有{94, 96, 12}，其中12将保留，因为它属于下一个顺串。当94输出且99读入时，由于99大于94，它必须属于第一个顺串。最终我们将得到两个顺串：第一个包含{11, 81, 94, 96, 99}，第二个包含{12, 35}。
你的任务是实现这个替换选择算法。
输入格式：
每个输入文件包含多个测试用例。第一行给出两个正整数N（≤105）和M（<N/2），分别是要排序的记录总数和内部内存的容量。然后在下一行给出N个整数，所有整数都在int类型范围内。一行中的所有数字之间用一个空格分隔。
输出格式：
对于每个测试用例，在每行中打印由替换选择算法生成的一个顺串（按升序排列）。一行中的所有数字之间必须恰好由一个空格分隔，并且行首和行尾不能有额外的空格。