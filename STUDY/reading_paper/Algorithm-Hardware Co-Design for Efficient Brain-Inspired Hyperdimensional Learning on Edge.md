- HDC：类脑超维计算To more closely model the human brain, earlier researchers proposed HyperDimensional Computing (HDC) as an alternative computing method, which mimics important brain functionalities towards high-efficiency and noise-tolerant computation.为了更贴近地模拟人脑，早期的研究者提出了多维的计算(多维的Computing，HDC )作为一种替代的计算方法，它模拟了重要的大脑功能，从而实现了高效率和抗噪声计算。
- bagging：一种集成方法
- $\text{KL}$ 算法是一种迭代改进的启发式算法。

| **阶段**            | **核心操作**                                                                                                 | **硬件平台**               | **关键创新/策略**                                                                          |
| ----------------- | -------------------------------------------------------------------------------------------------------- | ---------------------- | ------------------------------------------------------------------------------------ |
| **1. 编码**         | 将输入样本向量 $\vec{F}$ 映射为编码超向量 $\vec{E}$ 。                                                                   | **DNN 加速器** (Edge TPU) | 将 $\text{HDC}$ 编码映射为矩阵乘法（$\text{MAC}$ 操作），利用 $\text{TPU}$ 的并行性加速 。                   |
| **2. Bagging 采样** | 将原始训练数据集分解为 $M$ 个训练子集 。                                                                                  | **Host CPU**           | **Bagging** 策略的起始，用于生成多个弱学习器 。                                                       |
| **3. 训练**         | 根据编码后的超向量 $\vec{E}$ 更新类别超向量 $\vec{C}_k$ 。                                                                | **Host CPU**           | 利用 $\text{Bagging}$ 允许使用**更少的迭代次数** ($I'$) 和**更小的超向量宽度** ($d'$)，降低 $\text{CPU}$ 负载 。 |
| **4. 推理模型整合**     | 将 $M$ 个子模型的参数（基础超向量 $\mathbb{B}^m$ 和类别超向量 $\mathcal{C}^m$）堆叠成一个**单一的全尺寸推理模型** $\mathbb{B}, \mathbb{C}$ 。 | **Host CPU**           | 消除多模型加载开销，确保 $\text{TPU}$ 充分利用其并行性 。                                                 |
| **5. 推理**         | 使用整合后的单一模型进行分类 。                                                                                         | **DNN 加速器** (Edge TPU) | 实现实时高效预测 。                                                                           |
- 拓扑优化是指改变或调整模型的网络结构（拓扑）以适应特定的目标（如提高效率、适应硬件）。
- 硬件部署是指将模型（算法）高效且正确地映射到物理硬件（芯片）上。
- 负载均衡是指将计算任务合理分配给多个处理单元，以最大化吞吐量和资源利用率。





















---

### **[PPT 生成指令开始]**

**汇报主题：** **类脑计算模型在边缘硬件上的高效部署与优化方法研究**

**汇报结构：** **I. 文献一：协同设计** $\rightarrow$ **II. 文献二：通用编译器** $\rightarrow$ **III. 总结与展望**

---

### **Slide 1: 封面**

标题： 类脑计算模型在边缘硬件上的高效部署与优化

副标题： 基于算法-硬件协同设计方法和神经网络编译器方法

---

### **Slide 2: 本周文献阅读**

1. **本周阅读文献：**
    
    - **文献一：** 《Algorithm-Hardware Co-Design for Efficient Brain-Inspired Hyperdimensional Learning on Edge》1
        
    - **文献二：** 《Bridging the Gap Between Neural Networks and Neuromorphic Hardware with A Neural Network Compiler》2
        
2. **文献相关性：** 两篇文献探讨了如何解决类脑计算模型（HDC, SNN/ANN）在资源受限的边缘和神经拟态硬件上的**拓扑优化、硬件部署和负载均衡**问题。
    

---

### **Slide 3: 研究背景与挑战：边缘AI与类脑计算**

1. **趋势：** $\text{IoT}$ 和边缘计算的兴起，要求在资源受限设备上进行实时学习和认知任务 3。
    
2. **传统 DNN 挑战：** 训练复杂、参数量大，不适合边缘设备 4444。
    
3. **类脑/HDC 优势：** $\text{HDC}$（超维度计算）具有计算效率高、对噪声鲁棒的特性，是轻量级学习的潜在方案 5。
    
4. **核心挑战：** $\text{HDC}$ 的高维操作仍需高效硬件加速；神经拟态硬件的硬件约束（精度、规模）与 $\text{NN}$ 算法灵活性存在鸿沟。
    

---

### **第一部分：文献一汇报：算法-硬件协同设计 (HDC + Edge TPU)**

### **Slide 4: 研究动机与核心目标 (文献一)**

1. **挑战：** $\text{HDC}$ 需要大量的 $\text{MAC}$ 操作，在低功耗平台上成本高 6。现有 $\text{ASIC}$ 解决方案缺乏商业可行性且开发周期长 7。
    
2. **目标 (硬件部署)：** 提出一个框架，通过优化算法和硬件协同设计，利用现有、标准化 $\text{DNN}$ 加速器（如 $\text{Edge TPU}$）来加速 $\text{HDC}$ 学习 8。
    
3. **核心方法：** 将 $\text{HDC}$ 学习模型解释为一个**超宽神经网络 (Wide NN)**，以充分利用 $\text{Edge TPU}$ 的并行计算能力 9999。
    
    - **图示提示：**
        

### **Slide 5: TPU-CPU 协同设计工作流程**

1. **训练流程分解（负载均衡）：** 整个训练任务被划分为两部分，在不同的硬件上协同执行 1010。
    
2. **TPU 加速部分 (Encoding)：** 框架获取训练数据样本，并将其**编码 (Encoding)** 过程映射到 $\text{Edge TPU}$ 上 11。编码过程本质上是大量的 $\text{MAC}$ 操作（向量-矩阵乘法），利用 $\text{Edge TPU}$ 的 $\text{MXU}$ 进行高效并行加速 12121212。
    
3. **CPU 处理部分 (Model Update)：** 编码后的超向量被发送到主机 $\text{CPU}$ 13。由于 $\text{Edge TPU}$ 等边缘加速器不支持**权重更新**（即类别超向量的元素级更新操作），因此训练任务的这部分必须在 $\text{CPU}$ 上部署 14141414。
    
    - **图示提示：**
        

### **Slide 6: 推理流程与 Bagging 优化**

1. **推理流程（硬件部署）：** 框架加载基于已训练的类别超向量和基础超向量参数的推理神经网络模型，然后**完全映射到 $\text{Edge TPU}$ 上执行分类**（相似性检查，近似为点积），实现实时高效预测 15151515。
    
2. **Bagging 优化（拓扑优化/负载均衡）：** 为了减轻 $\text{CPU}$ 上训练更新的计算瓶颈，引入 **$\text{Bagging}$ 方法** 16161616。该方法允许使用**更少的训练迭代次数 ($I'$)** 和**更小的超向量宽度 ($d'=d/M$)**，显著降低 $\text{CPU}$ 上的计算成本，最高提速达 $4.74\times$ 171717171717171717。
    
3. **推理整合：** 训练完成后，所有子模型被堆叠组合成一个**单体全尺寸推理模型** 18181818，避免了多模型加载的额外开销，并充分利用 $\text{Edge TPU}$ 的并行性。
    
    - **图示提示：**
        

### **Slide 7: 性能总结**

1. **整体加速：** 相比基线 $\text{CPU}$ 平台，该框架实现了 $4.5\times$ 更快的训练速度和 $4.2\times$ 更快的推理速度 19191919。
    
2. **能效对比：** 相比功耗相近的嵌入式 $\text{ARM CPU}$ ($\text{Raspberry Pi 3}$)，训练提速 $19.4\times$，推理提速 $8.9\times$ 20202020。
    
3. **影响因素：** 框架的加速性能与**输入特征数量**正相关 21。特征数量少的应用（如 $\text{PAMAP2}$）加速效果不佳，这指导了 $\text{Edge TPU}$ 在应用场景上的选择 22222222。
    
    - **图表提示：** **和**
        

---

### **第二部分：文献二汇报：神经网络编译器的通用部署方法**

### **Slide 8: 研究动机与神经拟态硬件约束**

1. **背景：** 针对神经网络芯片（$\text{NN}$ chips）的开发面临硬件特定的约束，不同于通用处理器 23232323。
    
2. **主要约束：** 1. **信号/参数精度限制**（如 1-bit $\text{I/O}$, 8-bit 权重）24242424； 2. **计算规模限制**（如 $\text{TrueNorth}$ 的 $256 \times 256$ 矩阵）25252525； 3. **非线性函数种类限制**（如仅支持 $\text{LIF}$ 神经元模型）26。
    
3. **目标：** 提出一种通用方法学，通过编译器将**已训练的、无约束的 $\text{NN}$ 模型 ($G$)** 转换为满足目标硬件约束的等效网络 ($G'$) 27272727。
    
    - **表格提示：**
        

### **Slide 9: 现有解决方案及其局限性对比**

1. **方案一：硬件细节暴露（TrueNorth 方法）：** 直接暴露硬件限制，要求开发者从零开始构建和训练满足所有约束的模型 282828282828282828。
    
    - **局限性：** 模型与特定硬件紧密绑定，难以利用现有模型，且重新训练**耗时极长** 29292929。
        
2. **方案二：硬件满足软件：** 通过消耗更多硬件资源（如使用 16-bit 精度）来放松对 $\text{NN}$ 模型的约束 30。
    
    - **局限性：** 损失了从 $\text{NN}$ 量化和压缩中获得的性能提升，且某些物理限制（如模拟计算）难以克服 31。
        
3. **方案三：领域特定指令集 (ISA)：** 如 $\text{Cambricon ISA}$，需要软硬件同时满足 $\text{ISA}$ 要求 32。
    
    - **局限性：** 无法彻底解决 $\text{NN}$ 编程灵活性与硬件效率间的矛盾 33。
        

### **Slide 10: 编译器核心：解耦与图调优**

1. **核心方法论：** 通过编译器将**软件编程模型**（计算图 $\text{CG}$）与**硬件执行模型**（核心操作图 $\text{G}'$）解耦 343434343434343434。
    
2. **指导原则：**
    
    - **以规模换能力 ($\text{T.O.}$)：** 通过增大图的规模来弥补严格约束导致的性能损失 35353535。
        
    - **分而治之 ($\text{H.L.B.}$)：** 按拓扑顺序分步微调，促进快速收敛，避免误差累积 36363636。
        
3. **Graph Tuning 步骤：** **数据重编码**（解决 $\text{I/O}$ 精度）、**完全扩展**（解决计算规模）、**权重微调**（解决权重精度）37373737。
    
    - **图示提示：**
        

### **Slide 11: 拓扑优化：以规模换精度的实现**

1. **数据重编码（I/O 精度约束）：** 利用**自编码器**将浮点向量编码为低精度的硬件 $\text{I/O}$ 格式 38。
    
2. **机制：** 当 $\text{I/O}$ 精度极低时（如 $\text{1-bit}$），直接舍入损失信息。编译器通过增加**隐层神经元的数量**（$\text{re-encoding factor } m \times n$），即**增加计算规模**，来补偿精度损失，以维持准确率 39393939。
    
3. **权衡洞察：** $\text{NN}$ 模型对**低 $\text{I/O}$ 精度**的容忍度**低于低权重精度**，因为 $\text{I/O}$ 精度低会导致信号直接失真 40。因此，在 $\text{I/O}$ 精度极低时，软件需要消耗更多资源（如 $4\times$ 隐层神经元）来弥补 41414141。
    
    - **图示提示：** **和**
        

### **Slide 12: 硬件部署与负载均衡：Mapping**

1. **任务：** 将 $G'$ 图中满足约束的 $\text{core-ops}$ 高效地映射到目标硬件的物理核心上 42。
    
2. **负载均衡策略：** $\text{Mapping}$ 依赖于 $G'$ 中精细的**数据依赖**和**通信模式** 4343。例如：
    
    - 对于**权重驻留模式**（如 $\text{TianJi}$），使用 $\text{Kernighan-Lin (KL)}$ 划分算法将操作分配到物理核心，目标是**最小化跨分区边界的通信开销**，这是一种静态的资源优化和负载均衡 444444444444444444。
        
    - **权重的共享与重用**：将共享权重的 $\text{core-ops}$ (如卷积) 映射到同一物理核心，以减少数据移动 45。
        
3. **效率：** 转换过程耗时远少于重新训练（如 $\text{AlexNet}$ 转换约 1 小时，重训练约 3-4 天）46464646。
    

---

### **第三部分：总结与融合：对比与展望**

### **Slide 13: 总结：两种优化路径的对比与融合**

1. **TPU 协同设计 ($\text{HDC}$ 部署)：**
    
    - **优势**：商业可行性和快速部署，利用现有成熟硬件加速 $\text{HDC}$ 47。
        
    - **核心**：训练任务的 $\text{CPU}$/$\text{TPU}$ **功能划分**（$\text{H.L.B.}$）和 $\text{Bagging}$ **拓扑优化**（侧重训练加速）48484848。
        
2. **NN 编译器 (通用部署)：**
    
    - **优势**：通用性和对定制硬件的严格适配 49494949。
        
    - **核心**：图转换（**以规模换精度**）和 $\text{Mapping}$ 优化（侧重最小化**通信开销**）50505050。
        
3. **共同启发：**
    
    - **拓扑优化**：在两种路径中都至关重要（$\text{Bagging}$ 的水平划分 $\text{vs.}$ $\text{Fully Expanding}$ 的垂直分解）。
        
    - **负载均衡**：体现在**任务的功能划分**（异构协同）和**最小化通信开销**的静态分配上。
        

### **Slide 14: 未来研究方向展望（与导师方向结合）**

1. **拓扑优化 ($\text{T.O.}$)：** 借鉴 $\text{NN}$ 编译器的**图重构**和**精度-规模权衡**机制，探索如何将 $\text{Bagging}$ 等结构划分思想应用于更复杂的类脑拓扑结构。
    
2. **硬件负载均衡 ($\text{H.L.B.}$)：** 利用编译器方法提供的精细**数据流信息**，设计面向多核/$\text{NoC}$ 架构的**动态调度策略**，进一步提高资源利用率和实时性。
    
3. **硬件部署 ($\text{H.D.}$)：** 量化研究 $\text{I/O}$ 精度、权重精度与资源消耗的**设计权衡**，指导**神经拟态架构的联合设计**，提供最佳的硬件规格配置 51。
    

---

### **Slide 15: 总结与致谢**

1. **结论**：两篇文献分别展示了在不同硬件背景下解决 $\text{NN}$ 部署难题的有效系统级方法。
    
2. **致谢**：感谢老师的指导和建议。
    
3. **下一步计划**：根据文献启发，细化**神经拟态架构中的拓扑重构算法**和**基于数据流的动态负载均衡策略**。
    

---

**[PPT 生成指令结束]**