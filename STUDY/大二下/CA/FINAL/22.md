选择 
1. virtually index physically tag.给virtual address bits和physical address bits，给page size和cache size，以及cache的关联度，计算cache中存储的Tag是多少bit 
2. NUCA、SMP，这里是书上5.1部分的内容，我没看，哈哈T T 
3. consistency和coherence的定义 
4.  把一段指令整合成vector之后执行所需要的时钟周期。 
5. 1000条指令，40个L1miss，20个L2miss。其中L1 hit time = 1 cycle,L2 hit time = 10 cycle,miss penalty=200cycle.每个指令平均有1.5次memory访问，计算avg stall. 
6. VLIW由编译器静态调度，superscalar由硬件调度。 
7. 哪一段指令不能通过前递解决hazard
8. 计算实际的CPI，还是考cache miss那块的公式。 
9.  一些指令的执行顺序，哪个是不正确的。（RAW） 
10. NUMA，UMA，local memory & cache coherence；
11. TLB 情况下给你 pa len, va len, page size, cache size, cache type，怎么算 cache tag length（不是 TLB tag length）；
12. Memory Order 里面 weak order, total store order, partial store order 都是啥；
13. precise exception，imprecise exception，async exception，unordered exception；
14. 对已有的 cache type / size 等属性做修改，哪些能 save power，哪些能改善 Miss rate, miss penalty, hit time；
15. compulsory miss，collision miss，conflict miss；atomic lock，lr.d sc.d spinlock，spinlock 尽量减少 write（用 read 代替）；
16. 多重 cache 的 miss penalty 计算；VLIW 对比 superscalar；
17. double bump + pipelining，每个选项几条指令，问哪个的 hazard 可以靠 Forwarding 解决，具体是哪个 stage 的哪个 output，Forward 给谁；
18. RISC-V m 模式有无虚拟内存；AMAT 计算

大题 
1. directory based，给了cache和directory的状态，考一个processor要读写一个地址的block时进行的具体操作，要按步骤写出来。 
2. scorebroad和tomasulo & hardware speculation，给出一段汇编指令写出每个指令不同阶段的周期。 
3. cache，一段循环代码，不同cache大小和不同关联程度的情况下计算avg memory stall cycle？大概是这个，主要考察cache miss。
4. 在 write back 和 write alloc 策略下，一个 512 iter 的 for loop 里面是 32bit 的乘加 c_i = a_i * b_i + b_i（内存连续，2048bits a, 2048 bits b, 2048 bits c），如果全部 cache hit 那一个 iter 需要用 L cycle，read/write miss 100 cycle, 策略 write back + write alloc，考虑 1) 2KB cache size, 64B cache line, direct mapped 实际上的每 iter 需要多少 cycle，2) 32KB 4-way set assoc, 16B cache line 3) 32KB 2-way set assoc, 64B cache line 感觉很容易出错，完全没信心 
5. Scoreboard + Hardware speculation 
6. Directory Protocol 的协议消息内容 比较简单，但我没时间做完了，策略出现了问题 T T


补充： 
选择题： 两题考到precise exception的概念，可以通过互文猜出来 一堆basic cache optimization的题目，都是组成里面学的 还有一个用二维数组iteration考察空间局部性的miss计算，求blocked: unblocked比例（有待dl补充细节） 
大题来源： directory题目和jxh ppt的例题非常像 cache + iteration是书上例题B.8

补充一下那三个虚拟内存的计算吧 三个选择题题目很类似的 虚拟地址46bits，物理地址32bits，然后cache的tag用的物理地址，index用的虚拟地址，页表是8KB，然后cache用直接映射/4way-associattive/还有一个忘记是几路关联的 然后让计算cache的tag位数，index位数 （如果有误请指正） 还有一个tlb的问题，问的是tlb的index和数据分别是啥

补充scoreboard、speculation tomasulo指令序列： div x2,x3,x4 mul x1,x5,x6 add x3,x7,x8 mul x1,x1,x3 sub x4,x1,x5 sub x1,x4,x2 填每个指令在哪个周期IS/RO/EX/Write Result(scoreboard)、 IS/EX/WR/Commit(tomasulo) add/sub、mul、div部件各一个，每个部件一个reservation station （题目好像说了一个周期只能执行IS、dispatch、bus communication中的一个，不知道是三者不能同时执行，还是可以同时执行各一个）