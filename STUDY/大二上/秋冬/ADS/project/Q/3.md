Safe Fruit
### Project Description

Solve the problem No.1021 (safe fruit) in the PAT (top level) practice problem set ([link](https://pintia.cn/problem-sets/994805148990160896/exam/problems/type/7?problemSetProblemId=994805149506060288)).  
You should pass all the test cases on Pintia. You should also design your own test cases of different sizes, and see how the running time of your algorithm changes agianst the input size. Note that even on inputs of the same size, the algorithm may have different running time. Therefore, for each input size, you may evaluate your algorithm on multiple inputs, and then take the average runnning time.

### Submission Guidelines

You are required to upload a zip file that contains the project report (in pdf format), the source code ( It should include an independent file containing the program that can pass the test cases on pintia). The zip file should be named as PxGy.zip ( 'your student number + Px.zip' for personal bonus) where x is the project number (3 for this project) and y is your group number.

The report should be written clearly and neatly in English. It should starts with a title page containing the title, the group id, the author names, and an abstract summarizing your work. Within the 10 pages following the title page, the report should present the following contents.

- A description of the problem.
- A presentation of your algorithm. You should explain how you do branch and prune. You may get some ideas from this paper ([link](http://i.stanford.edu/pub/cstr/reports/cs/tr/76/550/CS-TR-76-550.pdf)). You should also give the pseudo-code of your algorithm. (Do NOT paste your actual code in this part!)
- An analysis of the running time of your algorithm. It may not be possible to derive a tight upper bound. Just do your best.
- An exhibition of your experiment and its result. You should state how the testing data is generated, the setting of your experiment, how the resulting data is obtained, as well as other things that you think are important. As to the result, you should give a table showing the running time of your algorithm on input of different size . You should also plot the running times against the input size for illustration.
- Discussion and Conclusion
- You may get a bouns if you come up with multiple algorithms and comparing their performance in your experiment. The bonus ranges from 0 to 1 pts depending on the quality of your work.

The report should be typeset using 12-point fonts, in a single-column, single-space format.

The source code should be in good style, and have sufficient comments.



## 一、项目描述（翻译）

解决 PAT 顶级练习题集中的第 1021 题《安全水果》（题目链接：[https://pintia.cn/problem-sets/994805148990160896/exam/problems/type/7?problemSetProblemId=994805149506060288](https://pintia.cn/problem-sets/994805148990160896/exam/problems/type/7?problemSetProblemId=994805149506060288)）。

- 需要通过该题目在拼题 A（Pintia）平台上的所有测试用例。
- 自行设计不同规模的测试用例，
	- 观察算法运行时间随输入规模的变化规律。注意，即使输入规模相同，算法运行时间也可能存在差异。因此，
	- 针对每种输入规模，需在多个输入用例上测试算法
- 计算运行时间的平均值。

## 二、提交指南（翻译）

- 压缩文件，内含项目报告（PDF 格式）和源代码（需包含一个独立文件，该文件中的程序需能通过拼题 A 平台的所有测试用例）。压缩文件命名格式为**PxGy.zip**（若为个人加分项，则命名格式为 “你的学号 + Px.zip”），其中 x 为项目编号（本次项目编号为 3），y 为你的小组编号。

项目报告需用英文撰写，内容清晰、格式规整。
- 报告需以标题页开头，标题页应包含项目标题、小组编号、作者姓名，以及一份概述你工作内容的摘要。标题页之后的内容需控制在 10 页内，且需涵盖以下模块：

1. 问题描述
2. 算法阐述：需说明分支剪枝策略的具体实现方式（可参考论文获取思路，论文链接：[http://i.stanford.edu/pub/cstr/reports/cs/tr/76/550/CS-TR-76-550.pdf](http://i.stanford.edu/pub/cstr/reports/cs/tr/76/550/CS-TR-76-550.pdf)）；同时必须给出算法的伪代码（本部分**禁止粘贴实际代码**）。
3. 算法运行时间分析：无需推导严格的上界，尽力完成分析即可。
4. 实验及结果展示：需说明测试数据的生成方式、实验环境设置、结果数据的获取方法，以及其他你认为重要的实验细节；结果部分需提供表格，展示算法在不同输入规模下的运行时间，同时需绘制运行时间随输入规模变化的图表，以便直观呈现。
5. 讨论与结论
6. 加分项：若能设计多种算法，并在实验中对比它们的性能表现，可获得额外加分。加分幅度为 0 - 1 分，具体根据工作质量评定。

报告排版要求：采用 12 号字体、单栏布局、单倍行距。

源代码要求：编码风格规范，包含充分的注释。

## 三、需完成的核心任务清单

1. **题目求解**
    - 编写程序解决 PAT 顶级第 1021 题，确保通过拼题 A 平台所有测试用例。
    - 设计不同规模的测试用例，针对每种规模构建多个输入样本，运行算法后计算平均运行时间，分析运行时间与输入规模的关联。
2. **文件准备与命名**
    - 撰写英文项目报告并导出为 PDF 格式，严格遵循排版规范（12 号字体、单栏、单倍行距）。
    - 整理源代码，包含一个独立的测试通过文件，代码需风格规范、注释充分。
    - 按要求命名压缩文件（团队项目：PxGy.zip；个人加分：学号 + Px.zip，x=3），将报告和源代码打包上传。
3. **报告内容完善**
    - 标题页包含完整信息（标题、小组号、作者名、工作摘要）。
    - 标题页后 10 页内完成五大核心模块（问题描述、算法阐述、运行时间分析、实验及结果、讨论与结论），若冲击加分项需增加多算法性能对比内容。
    - 算法阐述部分必须包含分支剪枝实现说明和伪代码，禁止出现实际代码。



1021 Safe Fruit

There are a lot of tips telling us that some fruits must not be eaten with some other fruits, or we might get ourselves in serious trouble. For example, bananas can not be eaten with cantaloupe (哈密瓜), otherwise it will lead to kidney deficiency (肾虚).

Now you are given a long list of such tips, and a big basket of fruits. You are supposed to pick up those fruits so that it is safe to eat any of them.

有很多小贴士提醒我们，有些水果不能和其他水果一起吃，否则可能会给身体带来严重不适。例如，香蕉不能和哈密瓜一起吃，否则会导致肾虚。

现在给你一长串这样的小贴士，以及一篮水果。你需要挑选出一些水果，使得任意食用其中的水果都是安全的。
### Input Specification:

Each input file contains one test case. For each case, the first line gives two positive integers: N, the number of tips, and M, the number of fruits in the basket. both numbers are no more than 100.

Then two blocks follow. The first block contains N pairs of fruits which must not be eaten together, each pair occupies a line and there is no duplicated tips; and the second one contains M fruits together with their prices, again each pair in a line. To make it simple, each fruit is represented by a 3-digit ID number. A price is a positive integer which is no more than 1000. All the numbers in a line are separated by spaces.

每个输入文件包含一个测试用例。对于每个测试用例，第一行给出两个正整数：N（小贴士的数量）和 M（篮子中水果的数量），两个数均不超过 100。

接下来是两个数据块。第一个数据块包含 N 对不能一起食用的水果，每对占一行，且没有重复的小贴士；第二个数据块包含 M 个水果及其价格，同样每对占一行。为简化问题，每个水果用一个 3 位数字的 ID 表示。价格为正整数，且不超过 1000。一行中的所有数字用空格分隔。
### Output Specification:

For each case, first print in a line the maximum number of safe fruits. Then in the next line list all the safe fruits, in increasing order of their ID's. The ID's must be separated by exactly one space, and there must be no extra space at the end of the line. Finally in the third line print the total price of the above fruits. Since there may be many different solutions, you are supposed to output the one with a maximum number of safe fruits. In case there is a tie, output the one with the lowest total price. It is guaranteed that such a solution is unique.

对于每个测试用例，首先在一行中输出安全水果的最大数量。然后在下一行按 ID 升序列出所有安全水果，ID 之间用恰好一个空格分隔，行尾不得有多余空格。最后在第三行输出上述水果的总价。由于可能存在多个不同的解决方案，要求输出安全水果数量最多的方案；若存在多个数量相同的方案，则输出总价最低的方案。题目保证这样的方案是唯一的。

### Sample Input:

```in
16 20
001 002
003 004
004 005
005 006
006 007
007 008
008 003
009 010
009 011
009 012
009 013
010 014
011 015
012 016
012 017
013 018
020 99
019 99
018 4
017 2
016 3
015 6
014 5
013 1
012 1
011 1
010 1
009 10
008 1
007 2
006 5
005 3
004 4
003 6
002 1
001 2
```

### Sample Output:

```out
12
002 004 006 008 009 014 015 016 017 018 019 020
239
```

代码长度限制

16 KB

时间限制

4000 ms

内存限制

64 MB
